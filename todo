- Solve When videos are unavailable
- Create add iframe as a method, that takes resources. Does not add several iframes to the same div, instead overrights the one view
- Move javascript logic to resources/js/
- Remove need for load/execute buttons (Query present onReady)
- Allow DL to finish before display
-Fix display (list of video should be shown b4 clicking)
